# Calliope TODO

## Foundation
- [x] Choose Qt/C++ as the primary application stack.
- [x] Add a starter stack for TDD and AI-assisted development.
- [ ] Split UI code from application/domain code as soon as the first real feature lands.
- [ ] Replace parked reference code in `main.cpp` with documented decisions or tracked tasks.

## Tooling
- [x] Enable a test-ready CMake structure.
- [x] Add a first Qt Test smoke test.
- [ ] Add a repeatable local workflow for configure, build, and test on Windows and Ubuntu 24.04.
- [ ] Decide whether to add formatting and linting tooling (`clang-format`, `clazy`, `clang-tidy`).

## Product
- [ ] Define the first user-facing feature slice to build with TDD.
- [ ] Decide what belongs in the main window versus a dedicated application service.
- [ ] Write the first behavior contract before implementing the first real feature.
